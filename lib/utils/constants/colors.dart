import 'package:flutter/material.dart';

class MyColors {
  // App theme colors
  static const Color appBarColor = Color(0xFF000000);

  // Container BackGround Color
  static const Color containerBackGroundColor = Color(0xFF33363D);

  // InputField FilledColor
  static const Color filledColor = Color(0xFFFFFFFF);

// Profile Circle Avatar
  static const Color circleAvatarBackGroundColor = Color(0xFF03A9F4);

// Text Color
  static const Color textColor = Color(0xFFBEBEBE);
}
